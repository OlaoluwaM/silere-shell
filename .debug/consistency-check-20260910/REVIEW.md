# Consistency check, September 10, 2026

Scope: working tree against HEAD, including untracked additions. Maintained changes span 19 files and 647 added/removed lines (604 additions, 43 removals); diagnostic snapshots are inspected separately. The change set introduces workspace navigation control flow and extends shared popup coordination, without new dependencies or settings keys.

## Findings fixed

- modules/menu/settings/SettingsWorkspacesSection.qml:14: the description promised an unqualified trailing empty workspace. It now describes the cap on Hyprland and keeps Niri wording unchanged, within the existing description width budget.
- modules/menu/BluetoothList.qml:21 and services/Bluetooth.qml:116,125,164: comments claimed model publication rebuilt every delegate. They now describe the current ScriptModel identity preservation, stable order while details are open, and removal of the affected row. No comments justified the stale claims.
- scripts/test-bluetooth-details.sh:1: the new runner was mode 0644 while neighboring runners are executable. It is now 0755.
- .debug/bluetooth-hardware-20260910/__pycache__: generated bytecode was removed from the worktree (moved to an owned temporary directory after rip failed). Diagnostic reports now link subsequent validation and distinguish historical source hashes from later comment cleanup.

## Cohesion

Workspace helpers stay beside existing private widget helpers and consume the compositor ownership boundary. Static and Niri navigation retain their existing path. Popup exclusivity uses the existing _claim/_opened functions and service arming mechanism. Bluetooth cleanup follows the existing Connections idiom and stable device identity. Regressions use probe-lib.sh lifecycle/error scanning with disposable module trees; replacement services are test fixtures only. The custom test trees are necessary because the shared _probe_project helper symlinks services that these tests must replace. Theme, motion, row sizing, registrations, and settings schema remain consistent. Cava removal remains deferred in TODO.md.

Discarded lint: none from the repository linter in this pass.
Required documentation: divergence ledger updated for all three behavioral changes; settings contract remains 52 declarations and 52 references with no changed keys.
Deeper review: code-review and the repository qt-qml-review are warranted for workspace control flow and shared popup state across this 19-file change set. The earlier Bluetooth review covers only its own fix. A new deeper review was not run as part of this consistency workflow.

Validation: ci-lint.sh and the full Nix check.sh both exit 0; the latter reports the same six environmental warnings. QML checks cover 198 files; regressions include 164 coordinator and seven Bluetooth assertions; the settings sweep covers 174 states over 55 surfaces. git diff --check passes. The running shell was not restarted and hardware disconnection was not repeated for this comment/copy/mode cleanup.
